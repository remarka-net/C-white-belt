#include <math.h>
#include <iostream>
using namespace std;

int main(){
	int a,b;

    while(a > 0 && b > 0)

        if(a > b)
            a %= b;

        else
            b %= a;

    return a + b;
    cout << a+b;
}
