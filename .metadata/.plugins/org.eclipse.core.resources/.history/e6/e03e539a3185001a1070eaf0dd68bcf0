#include <math.h>
#include <iostream>
#include <vector>
using namespace std;

int main() {
	int n;
	vector<int> v;
	n = 5;
	for (int i=0; n<2; i++){
		int x;
		x = n%2;
		n = n/2;
		v.push_back(x);
	}
	  for(int k = 0; k < v.size(); k++){
	        cout << v[k] << ' ';
	  }
	return 0;
}
